import * as React from 'react';
import Village from 'data/data/Village';
import { ViewContext } from 'view/viewContext';
import { IconGoTo } from 'view/components/icon';

/******************************/

type ComponentProps = Readonly<{
    village: Village,
    context: ViewContext,
    children?: React.ReactNode
}>;
type ComponentState = Readonly<{}>

/******************************/

export default
class GoToBtn
extends React.Component<ComponentProps, ComponentState> {

    onClickBtn = (ev: React.MouseEvent): void => {
        let { x, y } = this.props.village;

        this.props.context.mainCanvas.commands.goTo(x, y);
        ev.stopPropagation();
        ev.preventDefault();
    }

    /******************************/

    render(): JSX.Element {
        return (
            <button
                className="btn btn-dark btn-xs"
                onClick={ this.onClickBtn }
            >
                <IconGoTo />
            </button>
        );
    }
}
