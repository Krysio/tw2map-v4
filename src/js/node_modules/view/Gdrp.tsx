import * as React from 'react';
import { ViewContext } from 'view/viewContext';
import { IconSettings } from 'view/components/icon';

/******************************/

type ComponentProps = Readonly<{
    context: ViewContext,
    children?: React.ReactNode
}>;
type ComponentState = Readonly<{}>;

/******************************/

export default
class Gdrp
extends React.Component<ComponentProps, ComponentState> {

    showSettings = (): void => {
        this.props.context.openSettings('analytics');
    }

    close = (): void => {
        const globalStore = this.props.context.stores.global;

        globalStore.actions.setGdrpShown(true);

        this.forceUpdate();
    }

    /******************************/

    render(): React.ReactNode {
        let globalStore = this.props.context.stores.global,
            state = globalStore.getState();

        if (state.analytics.gdrpShown) {
            return null;
        }

        return (
            <div
                className="modal modal-gdrp"
                style={{
                    display: 'block'
                }}
            >
                <div className="modal-dialog">
                    <div className="modal-content">
                        <div className="modal-body">
                            <p>
                                It looks like you are from the UE territory.<br />
                                So, I'm obliged to inform your's right to privacy settings choice.
                            </p>
                            <p>
                                You can change your settings here: &nbsp;
                                <button
                                    className="btn btn-primary"
                                    onClick={ this.showSettings }
                                    role="button"
                                    aria-pressed="false"
                                    aria-label="Settings"
                                >
                                    <IconSettings />
                                </button>
                            </p>
                            <p>
                                Privacy setting in default are not restricted.
                            </p>
                        </div>
                        <div className="modal-footer">
                            <button
                                className="btn btn-primary"
                                onClick={ this.close }
                                role="button"
                                aria-pressed="false"
                                aria-label="Close"
                            >
                                OK
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        );
    }
}
