import Events from "data/Events";
import MapData from "data/data/MapData";

/******************************/

export default function commandCleanData(
    events: Events,
    mapData: MapData
) {
    return {
        clean(): void {
            mapData.clearMapData();

            events.emit('changed data/background', null);
        }
    };
}