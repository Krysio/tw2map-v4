import { EventEmitter } from "events";

/******************************/

export default class EventBox extends EventEmitter {
    emit(command: 'map/hover', data: {x: number, y: number}): boolean;

    emit(command: string, data: any): boolean {
        return super.emit(command, data);
    }

    /******************************/

    on(command: 'map/hover', listener: (data: {
        x: number,
        y: number
    }) => void): this;

    on(command: string, listener: (data: any) => void): this {
        return super.on(command, listener);
    }
}