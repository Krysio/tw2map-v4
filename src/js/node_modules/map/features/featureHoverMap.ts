import Events from 'map/Events';
import Village from 'map/data/MapData/Village';
import MapData from 'map/data/MapData';

/******************************/

export default function featureHoverMap(
    events: Events,
    mapData: MapData
): void {
    events.on('hover/map', ({ x, y }: {x: number, y: number}) => {
        let village: Village = mapData.getVillageByCoords(x, y);

        events.emit('hover/item', village);
    });
}