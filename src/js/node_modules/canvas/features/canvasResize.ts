import { Commands } from "canvas";

/******************************/

export default function featureCanvasResize(
    commands: Commands
): void {
    window.addEventListener(
        'resize',
        commands.setParentElementSize,
        false
    );
    commands.setParentElementSize();
}