import StoreWrapper from 'stores/StoreWrapper';
import * as globalStore from 'stores/global';

/******************************/

export type StoreMap = {
    global: StoreWrapper<globalStore.State, globalStore.Actions>
};

/******************************/

export default function createStores(): StoreMap {
    return {
        global: globalStore.createStore()
    }
}
