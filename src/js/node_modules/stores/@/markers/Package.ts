import BaseState from "stores/@/BaseState";

/******************************/

export default class Package extends BaseState {
    name: string;
    root = {};

    constructor(
        argUiid?: string,
        argName?: string
    ) {
        super();
        this.uuid = argUiid || this.uuid;
        this.name = argName || 'Unnamed';
    }
}