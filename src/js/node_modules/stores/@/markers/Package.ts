import uuid from 'uuid/v4';

export default class Package {
    uuid: string;
    name: string;
    root = {};

    constructor(
        argUiid?: string,
        argName?: string
    ) {
        this.uuid = argUiid || uuid();
        this.name = argName || 'Unnamed';
    }
}