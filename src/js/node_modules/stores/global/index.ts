import StoreWrapper from 'stores/StoreWrapper';

import { reducer } from './reducer';
import { State } from './types';
import { actions } from './actions';

/******************************/

type Actions = typeof actions;

/******************************/

export default function createWrapperStore(): StoreWrapper<State, Actions> {
    return new StoreWrapper<State, Actions>(reducer, actions);
};
export { createWrapperStore as createStore, State, Actions };
