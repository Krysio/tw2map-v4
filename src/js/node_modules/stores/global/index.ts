import StoreWrapper from 'stores/StoreWrapper';

import { reducer } from './reducer';
import { WState, wrapper } from './@';
import { actions } from './actions';

/******************************/

type Actions = typeof actions;

/******************************/

export default function createGlobalStore() {
    return new StoreWrapper<WState, Actions, WState>(reducer, actions, 'global', wrapper);
};
export type GlobalStore = ReturnType<typeof createGlobalStore>;
export { createGlobalStore as createStore, WState as State, Actions };
