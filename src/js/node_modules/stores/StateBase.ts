export default class StateBase {
    copy(): StateBase {
        let stateConstructor = this['__proto__'].constructor,
            copyState = new stateConstructor();

        Object.assign(copyState, this);

        return copyState;
    }
}