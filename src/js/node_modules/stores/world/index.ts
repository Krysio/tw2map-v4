import StoreWrapper from 'stores/StoreWrapper';

import { reducer } from './reducer';
import { WState, wrapper } from './@';
import { actions } from './actions';

/******************************/

type Actions = typeof actions;

/******************************/

export default function createWorldStore() {
    return new StoreWrapper<WState, Actions, WState>(reducer, actions, 'world', wrapper);
};
export type WorldStore = ReturnType<typeof createWorldStore>;
export { createWorldStore as createStore, WState as State, Actions };
