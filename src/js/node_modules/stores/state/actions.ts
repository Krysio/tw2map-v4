export const actions = {
    setMapKey(
        marketKey: string,
        worldKey: string,
        command: string,
        value: string
    ): void {
        this.dispatch({
            type: 'setMapKeys',
            marketKey, worldKey,
            command, value
        });
    }
}
