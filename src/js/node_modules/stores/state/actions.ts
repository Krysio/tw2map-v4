export const actions = {
    setMapKey(
        marketKey: string,
        worldKey: string
    ): void {
        this.dispatch({
            type: 'setMapKeys',
            marketKey,
            worldKey
        });
    }
}
